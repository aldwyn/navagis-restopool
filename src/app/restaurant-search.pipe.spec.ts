import { RestaurantSearchPipe } from './restaurant-search.pipe';

describe('RestaurantSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RestaurantSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
